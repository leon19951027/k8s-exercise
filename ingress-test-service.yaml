apiVersion: v1
kind: Service
metadata:
  name: ingress-test-service
  namespace: ingress-test
spec:
  type: NodePort
  ports:
  - name: http-port
    nodePort: 30010
    port: 80
    targetPort: 80
  selector:
    app: ingress-test-pod
